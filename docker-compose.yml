services:
  meilisearch:
    image: getmeili/meilisearch:v1.48.3
    restart: unless-stopped
    environment:
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:-book-id-search-dev-key}
      MEILI_ENV: ${MEILI_ENV:-development}
    ports:
      - "${MEILI_PORT_BIND:-127.0.0.1:7700}:7700"
    volumes:
      - ${MEILI_DATA_DIR:-./meili_data}:/meili_data

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    depends_on:
      - meilisearch
    environment:
      MEILI_HOST: http://meilisearch:7700
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY:-book-id-search-dev-key}
      MEILI_INDEX: ${MEILI_INDEX:-books}
      API_PORT: 3001
      BOOK_DATA_DIR: /data/private
    ports:
      # Bound to host loopback only. Public access is via Caddy → 127.0.0.1:5173 → web nginx → api.
      # Do NOT change to "0.0.0.0" or remove the loopback prefix; see reports/SECURITY_HARDENING.md.
      - "127.0.0.1:${API_PORT:-3001}:3001"
    volumes:
      - ${BOOK_DATA_DIR:-./data}:/data/private:ro
      - ./reports:/app/reports

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        VITE_API_BASE_URL: /api
    restart: unless-stopped
    depends_on:
      - api
    ports:
      # Bound to host loopback only. Caddy → 127.0.0.1:5173 (host) → web container :80.
      - "127.0.0.1:${WEB_PORT:-5173}:80"
